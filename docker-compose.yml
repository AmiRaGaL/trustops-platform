services:
  postgres:
    image: postgres:16-alpine
    container_name: trustops-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: trustops
      POSTGRES_PASSWORD: trustops
      POSTGRES_DB: trustops
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trustops -d trustops"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: trustops-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
